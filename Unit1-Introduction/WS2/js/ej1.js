{ /* we calculate the age of a person */
    let currentYear = 2021;
    let birthYear = 2001;

    let maxAge = currentYear-birthYear;
    let minAge = maxAge - 1;

    console.log("They are either " + minAge + " or " + maxAge);
}